**Avatar** — initials on a neutral circle (Block 23 has no client photos at MVP). `accent` is the violet-tinted signed-in-owner style.

```jsx
<Avatar initials="MS" />
<Avatar initials="MR" accent size={34} />
<Avatar initials="JR" dim />   {/* archived row */}
```
